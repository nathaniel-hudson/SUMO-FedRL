from os.path import join

ranked_policy_checkpoint = join(
    "out", "models", "Simple", "ranked-fedrl", "checkpoint_100"
)

unranked_policy_checkpoint = join(
    "out", "models", "Simple", "unranked-fedrl", "checkpoint_100"
)
